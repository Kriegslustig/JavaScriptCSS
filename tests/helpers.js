var assert = require('assert')
var helpers = require('../helpers/helpers.js')

describe('helpers', function () {
  describe('$', function () {
    it('should be able to parse an expression and return the pased value', function () {
      assert.equal(true, helpers.$(true))
    })
  })

  describe('extend', function () {
    it('should be an object')
    it('should have a function add')
    it('should have a function that')
    describe('add', function () {
      it('should take two strings as arguments')
      it('should take two strings as arguments')
      it('should add functions to extend.extendables')
    })
    describe('that', function () {
      it('should only take strings for which there have been extendables added for as an argument')
      it('should add a hook to onDone')
    })
    it('should add css blocks to the end of a file')
  })
})
